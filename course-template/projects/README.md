# Projects

Each project lives in its own folder here:

```
projects/
  project-1/
  project-2/
  final-project/
```

Create the folder when you start the project. Every project needs its own `README.md` with setup notes, the audience/problem it serves, and data-source attribution where relevant — see the project brief in Canvas.
